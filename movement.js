class movment{
    constructor(local,movment,speed){
        this.local = local;
        this.movment = movment;
        this.speed = speed;
    }
   move(){
       this.translateModel(temp,)
   }
    translateModel(offset){
        vec3.add(translation,translation,offset);
    }
}